.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install littleauth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv littleauth
    $ pip install littleauth
